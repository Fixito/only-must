[build]
command = "pnpm install --frozen-lockfile && pnpm build:web"
publish = "apps/web/dist/client"

[build.environment]
NODE_VERSION = "22"

[context.production]
ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- apps/web packages/shared package.json pnpm-lock.yaml"
